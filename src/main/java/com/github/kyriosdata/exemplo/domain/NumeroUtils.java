// Incluido por GFT AI Impact Bot
// Adicionado verificação de nulidade para evitar NullPointerException
if (trechos == null || trechos.isEmpty()) {
    throw new IllegalArgumentException("trechos não pode ser nulo ou vazio");
}

// Incluido por GFT AI Impact Bot
// Adicionado verificação de nulidade para evitar NullPointerException
if (separador == null) {
    throw new IllegalArgumentException("separador não pode ser nulo");
}

// Incluido por GFT AI Impact Bot
// Adicionado verificação de nulidade para evitar NullPointerException
if (trecho == null) {
    throw new IllegalArgumentException("trecho não pode ser nulo");
}

// Incluido por GFT AI Impact Bot
// Adicionado verificação de nulidade para evitar NullPointerException
if (valor == null) {
    throw new IllegalArgumentException("valor não pode ser nulo");
}

// Incluido por GFT AI Impact Bot
// Adicionado verificação de nulidade para evitar NullPointerException
if (numero == null) {
    throw new IllegalArgumentException("numero não pode ser nulo");
}

// Incluido por GFT AI Impact Bot
// Adicionado verificação de nulidade para evitar NullPointerException
if (cdu == null) {
    throw new IllegalArgumentException("cdu não pode ser nulo");
}

// Incluido por GFT AI Impact Bot
// Adicionado verificação de nulidade para evitar NullPointerException
if (du == null) {
    throw new IllegalArgumentException("du não pode ser nulo");
}

// Incluido por GFT AI Impact Bot
// Adicionado verificação de nulidade para evitar NullPointerException
if (unidade == null) {
    throw new IllegalArgumentException("unidade não pode ser nulo");
}

// Incluido por GFT AI Impact Bot
// Adicionado verificação de nulidade para evitar NullPointerException
if (valor == null) {
    throw new IllegalArgumentException("valor não pode ser nulo");
}

// Incluido por GFT AI Impact Bot
// Adicionado verificação de nulidade para evitar NullPointerException
if (valor == null) {
    throw new IllegalArgumentException("valor não pode ser nulo");
}